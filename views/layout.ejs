<!DOCTYPE html>
<html lang=es>
  <head>
    <meta "charset=UTF-8">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet'
          media='only screen and (min-device-width: 481px)'
          href='/stylesheets/wide.css' />
    <link rel='stylesheet'
          media='only screen and (max-device-width: 480px)'
          href='/stylesheets/smartphone.css' />
  </head>
  <body>
    <div id="page-wrap">

      <header class="main" id="h1">
        <h1>Quiz: <small>el juego de las preguntas</small></h1>
        <% if(!session.user) {%>
          <span class="right"><a href="/login">Login</a></span>
        <% } else { %>
          <span class="right"><%= session.user.username%><a href="/logout">Logout</a></span>
        <% }%>
      </header>

      <nav class="main" id="n1" role="navigation">
        <span><a href="/">Inicio</a></span>
        <span><a href="/quizes">Preguntas</a></span>
        <span><a href="/author">Créditos</a></span>
        <span><a href="/quizes/statistics">Estadísticas</a></span>
      </nav>

      <section class="main" id="s1">

        <% if (errors.length) { %>
        <span id="ErrorMsg">
          <% for (var i in errors) { %>
            <span> <%= errors[i].message %></span><br>
          <% } %>
        </span>
        <% } %>

        <div>
          <%- body %>
        </div>
      </section>

      <footer class="main" id="f1">
        <a href="http://es.wikipedia.org/wiki/Quiz">¿Que es un Quiz?</a>
        <a href="https://github.com/adriangmweb/express-quiz-app"> Ver proyecto en GitHub</a>
      </footer>

    </div>
  </body>
</html>
